# -*- coding: utf-8 -*-
"""实验作业二报告的长文本素材（思考题作答、AI 协作记录、结论），供 fill_report.py 调用。"""

# ---------------- 1.1 实验目的 ----------------
PURPOSE = [
    "1. 理解 ConvLSTM“把 LSTM 中的矩阵乘法替换为二维卷积”的核心思想：普通 LSTM 的隐状态是向量，"
    "ConvLSTM 的隐状态是与输入同尺寸的特征图，从而在时序建模之外额外保留了空间结构；"
    "掌握“看过去若干帧、预测下一帧”这一时空序列预测任务的标准建模流程（数据形状 (B,T,C,H,W)、沿时间循环喂帧、逐时刻滚动状态）。",

    "2. 掌握 ConvLSTM 细胞的参数量计算公式，能对给定配置逐项手算（每个门 K×K×C_in×C_h + K×K×C_h×C_h + C_h，"
    "细胞总计为其 4 倍；输出卷积 3×3×C_h×1 + 1），并与 sum(p.numel() for p in model.parameters()) 的代码结果核对一致，"
    "理解“容量—成本”的平方级权衡。",

    "3. 在脚本自合成的弹跳小球序列上跑通基线 ConvLSTM，并通过 6 组严格控制变量的对照实验"
    "（层数、隐藏通道、卷积核、输入帧数、网络结构、损失函数），定量理解各设计维度对预测质量的影响；"
    "最后把被验证有效的改进合成最优组合并固定种子复测 3 次，用预测帧对比图直观评价预测质量。",
]

# ---------------- 4. AI 协作记录 ----------------
COOP_ROWS = [
    ("实验框架生成（对应 Builder）",
     "“请在当前目录创建 convlstm_bounce.py，实现弹跳小球时空预测实验：1) NumPy 生成 2000+200 条 10 帧 32×32 序列，"
     "小球半径 2、随机初速度 0.8~1.6、边界反弹、随机种子 42；2) 定义 ConvLSTMCell，用一个 "
     "Conv2d(in_ch+hid_ch, 4*hid_ch, k, padding=k//2)，前向中 cat([x,h]) 后过卷积、chunk 成 4 份分别过 Sigmoid/Tanh，"
     "按 c_t=f*c+i*g、h_t=o*tanh(c) 更新；3) 定义 ConvLSTM 模型，层数/hidden/kernel 可配置；"
     "4) MSE+Adam(0.001)、批 64、5 epoch，逐 epoch 记录训练损失与测试 MSE/MAE；"
     "5) 保存损失曲线与‘输入帧|真实帧|预测帧’对比图，中文字体用 Microsoft YaHei。”（指南步骤 1 原文）",
     "AI 一次性生成完整框架，与指南参考代码结构一致。我逐行通读后重点核查了四处并做了修改："
     "① 指南与 AI 初稿都把训练集写成 data[:2000]、测试集写成 data[1800:]，二者重叠 200 条，构成数据泄漏；"
     "我改为生成 2200 条序列并按 [:2000] / [2000:] 切分，严格执行“2000 训练 + 200 测试”。"
     "② 增加 (B,T,C,H,W) 的形状断言，把指南 Q3 提到的 expected 5D input 坑挡在训练之前。"
     "③ 把随机数生成器显式绑定种子（torch.Generator），避免 DataLoader 式乱序带来的不可复现。"
     "④ 设 KMP_DUPLICATE_LIB_OK 环境变量规避本机 OpenMP 重载冲突。"),
    ("参数量手算与公式核对（对应 Chat）",
     "“我把 ConvLSTM 细胞合并成一个卷积：Conv2d(C_in+C_h, 4*C_h, K, padding=K//2)。"
     "请推导它的参数量公式，并说明 4 个门的顺序 chunk 成 i,f,g,o 是否会影响语义；"
     "再帮我核对 K=3、C_in=1、C_h=32 时的细胞与输出卷积参数量。”",
     "AI 给出的公式与 4.2 节一致：4×(K²C_in C_h + K²C_h² + C_h)。我采纳后自己代入算得 4×(288+9216+32)=38144，"
     "输出卷积 3×3×32+1=289，总计 38433，与代码 sum(p.numel()) 完全一致。"
     "AI 同时提醒 chunk 顺序必须与公式的 i,f,g,o 严格对应（g 用 tanh，i/f/o 用 sigmoid）。"
     "我把这一条作为重点逐行核对：顺序写错时模型依然能正常运行、损失也会下降，"
     "仅靠“不报错”根本无法发现语义错误，因此最终是回到公式逐项比对门控顺序后才确认无误——"
     "这也是我全程坚持自己读懂公式的原因。"),
    ("形状报错定位与最优组合设计（对应智能修复 + Chat）",
     "“报错 RuntimeError: Expected 4D (unbatched) or 5D (batched) input to conv2d, but received 4D/3D …，"
     "形状为 torch.Size([64, 32, 32])；另外请说明在‘看 4 帧预测第 5 帧’任务上，"
     "输入帧数、隐藏通道、卷积核这三个维度哪个更值得优先加大？”",
     "AI 立刻定位为“忘记沿时间维逐帧取 x[:, t]”——直接整批送入 conv2d 会把时间维误当通道维；"
     "并指出数据生成后必须 permute(0,1,4,2,3) 把 (N,T,H,W,1) 换成 (B,T,C,H,W)。采纳后我在 ConvLSTM.forward 内"
     "加了 5D 形状断言，此后再未复现该错。关于设计取舍，AI 提示“输入帧数直接决定可观测的运动信息量，"
     "通常优先于容量扩张”——这一建议被实验 4 直接推翻：固定 5 epoch 预算下看 8 帧反而使 MSE 持平、MAE 恶化 29%。"
     "这件事对我的意义比一次成功的建议更大：AI 给的是符合直觉的一般性经验，是否成立必须由本任务的对照实验判定，"
     "最终我只把被实验证实有效的三项（加深层数、隐藏通道、卷积核）纳入最优组合。"),
]

# ---------------- 6. 思考题作答 ----------------
QA = [
    # (1)
    ["本质区别在于“状态与输入之间的运算形式”：普通 LSTM 用矩阵乘法（全连接）计算门控，"
     "i_t = σ(W_xi·x_t + W_hi·h_{t−1} + b_i)；ConvLSTM 把矩阵乘法全部换成二维卷积，"
     "i_t = σ(W_xi ∗ X_t + W_hi ∗ H_{t−1} + b_i)，f、o、g 四门同理。"
     "由此带来两个结构性差异：① 隐状态与细胞状态的形态不同——LSTM 是 C_h 维向量，"
     "ConvLSTM 是 C_h×H×W 的二维特征图；② 参数的组织方式不同——LSTM 为每个输入元素单独配权重，"
     "ConvLSTM 用同一个卷积核在全图上滑动，实现“局部感受野 + 参数共享”。"
     "不过细胞状态更新式完全不变：C_t = f_t ⊙ C_{t−1} + i_t ⊙ g_t，H_t = o_t ⊙ tanh(C_t)。",

     "保留二维空间结构对运动序列特别重要，原因有三："
     "① 空间邻接信息不被破坏——32×32 图像展平成 1024 维向量后，相邻像素被拆散，"
     "“小球是一个半径 2 的连通团块”“位置有平移关联”这类先验无法被模型直接利用；"
     "卷积核只在 3×3 邻域内聚合，天然保留上下左右的邻接关系。",
     "② 平移等变性匹配运动预测的物理本质——预测下一帧的核心是估计一个位移矢量，"
     "这是一个局部且平移等变的操作：同一个卷积核无论小球在左上角还是右下角都能检测到它，"
     "模型无需为每个位置学一套独立权重。"
     "③ 参数量与分辨率解耦——LSTM 的参数量随空间尺寸平方增长（本实验把 4 帧展平后输入维度 4096，"
     "仅 weight_ih 一项就有 419 万参数），ConvLSTM 的参数量只由 K 与 C_h 决定、与 H、W 无关，"
     "换成 128×128 输入参数量仍是 38433。实验 5 用全连接 LSTM 在参数量为基线 122.9 倍的情况下"
     "仍取得了更差的 MSE，正是“丢弃空间局部性后样本效率骤降”的直接实证。"],
    # (2)
    ["代入公式：单细胞参数量 = 4×(K×K×C_in×C_h + K×K×C_h×C_h + C_h)，"
     "其中 K=3、C_in=1、C_h=64：",
     "= 4×(3×3×1×64 + 3×3×64×64 + 64) = 4×(576 + 36864 + 64) = 4×37504 = 150016。"
     "与 C_h=32 时的 38144 相比，增长 150016 / 38144 ≈ 3.93 倍（约 4 倍）。"
     "若把输出卷积一并计入，总量由 38433 增至 150593，同样是约 3.92 倍。",
     "为什么恰好约 4 倍：4 个门中占主导的是 K²C_h² 项，它随 C_h 呈平方增长，"
     "C_h 翻倍即变为 4 倍（9216 → 36864）；K²C_in C_h 项只随 C_h 线性增长（288 → 576），"
     "但因 C_in=1 使其仅占总量的 3% 左右，对总倍数的拉动可忽略。"
     "容量与成本的权衡：隐藏通道决定了网络能同时容纳的时空模式数量，容量约随 C_h² 增长，"
     "但前向计算量（FLOPs）同样近似按 C_h² 增长，显存与单 epoch 耗时也同步上升——"
     "本实验中 C_h=64 的耗时约为基线的 4 倍。因此在算力受限时，"
     "“加大 C_h”必须用 MSE 的相对降幅来交换，只有当降幅显著大于成本增长时才值得；"
     "实践上更常见的做法是先保证输入信息量（输入帧数）与合作结构（卷积核），再谨慎扩张通道。"],
    # (3)
    ["全连接 LSTM 的参数量：LSTM 部分 4×256×(4096+256) + 8×256 = 4458496（含 weight_ih 4194304、"
     "weight_hh 262144、两个偏置各 1024），输出层 Linear(256→1024) 为 256×1024 + 1024 = 263168，"
     "合计 4721664。该数值已用代码核对一致。作为对比：基线 ConvLSTM 仅 38433 个参数，"
     "全连接 LSTM 是它的 122.9 倍，也是 C_h=64 版 ConvLSTM（150593）的 31.3 倍。",

     "从参数量看：训练集只有 2000 条序列，4.7M 参数意味着平均每条样本要支撑约 2361 个参数，"
     "模型有充足能力把训练集“背下来”，偏差小但方差极大，泛化必然受损；"
     "而 ConvLSTM 用 38433 个参数就覆盖了同一任务，参数效率高出两个数量级。",

     "从空间局部性看：展平操作把 32×32 的二维网格变成无序的 4096 维向量，"
     "相邻像素在向量中虽然位置相邻，但全连接层并不利用这一先验——它给每个像素位置单独配一套权重，"
     "等价于要求网络用 1024 维查表的方式记住“任意初始位置 → 下一帧位置”的映射。"
     "本实验中小球的初始位置是随机均匀采样的，位置组合极多，查表式学习需要成倍的样本才能覆盖，"
     "这正是它 MSE 更差的核心原因。ConvLSTM 则依靠同一组 3×3 卷积核在全图滑动，"
     "把“局部形状 + 平移等变”直接编码进结构，用极少参数就获得了更好的预测。",
     ],
    # (4)
    ["原理上，增加输入帧数能提升精度的机制有三个方面。（需要先说明一点：本实验在固定 5 epoch / 155 步的"
     "优化预算下并未观察到收益——看 8 帧使 MSE 持平、MAE 反而恶化 29%，原因见 3.4 节；"
     "因此下面回答的是“为什么理论上应当有效”以及这种收益得以兑现的前提条件。）"
     "① 运动信息更充分、对量化误差更鲁棒——"
     "序列中球心坐标只有整数像素分辨率，单帧位移（0.8~1.6 像素）被四舍五入后带有明显的量化噪声；"
     "只看 4 帧等于用 3 个位移样本估速度，噪声被放大。8 帧给出更长的位移基线，"
     "相当于用最小二乘意义下的斜率估计速度，速度大小与方向的估计方差显著下降，"
     "因此预测位置更准、更稳定。",
     "② 更容易识别边界反弹。判断“反弹”必须观察到位移方向发生反转，"
     "帧数少时反弹点前后只有 1~2 帧，网络难以区分“继续匀速”与“折返”；"
     "帧数增加后反弹前后的完整轨迹都能进入感受野，模型能学到方向翻转这一事件。"
     "③ 隐状态沿时间滚动，帧数增加等价于扩大时序感受野，"
     "f 门可以把更早时刻的速度信息保留在 C_t 中，缓解单帧信息不足的问题。",
     "如果继续加到 16 帧，会遇到以下问题："
     "① 本实验序列总长只有 10 帧，取 16 帧输入根本无处可取，必须先把序列加长到 18 帧以上，"
     "数据合成规模与训练时间同步上升。"
     "② 计算与显存成本随 T 线性增长：ConvLSTM 沿时间循环调用细胞，前向与反向的卷积次数都正比于 T，"
     "16 帧相对 8 帧几乎翻倍。"
     "③ 时序梯度路径变长，门控饱和会造成信息衰减，更易出现梯度消失/爆炸；"
     "虽然 C_t 的加法通路缓解了这一点，但长期依赖仍是难点。"
     "④ 边际收益递减：小球运动是“匀速直线 + 偶尔反弹”的一阶马尔可夫过程，"
     "8 帧已足以覆盖一个完整的反弹周期，继续加帧只是在重复相同信息，"
     "而窗口内有效样本数（固定序列长度下可取的起点数）反而变少，性价比明显下降。"
     "⑤ 冗余输入还可能让网络把帧间无关的相位噪声一起学进去，带来过拟合风险。"],
    # (5)
    ["在气象降水临近预报（雷达回波外推）中，ConvLSTM 的输入是一段历史的雷达回波反射率场序列，"
     "形状为 (T, C, H, W)：例如过去 1 小时每 6 分钟一帧共 10 帧，"
     "单通道为组合反射率（或接入多个仰角、双偏振量构成多通道），"
     "空间分辨率约 1 km、区域范围可达 512×512；常额外拼接地形高程、经纬度网格与时间编码作为辅助通道。"
     "输出是未来若干时刻的同构反射率场，例如未来 0~2 小时逐 10 分钟的 (T', 1, H, W) 序列，"
     "再经阈值化或定量降水估计（QPE）关系转换为降水强度栅格。"
     "可见其任务形式与弹跳小球完全一致（看图序列 → 预测下一张图），"
     "但输入输出承载的物理含义和难度有本质差别。",

     "该任务相对弹跳小球的额外难点主要有四类："
     "① 目标可生消（birth / death）。降水回波不是守恒刚体，会因地形抬升、辐合生成，"
     "也会因蒸发、下沉而消散，模型必须支持“从无到有、从有到无”；"
     "而小球是守恒的刚性目标，只会移动，绝不会凭空出现或消失，"
     "所以小球任务上的误差主要来自位置估计，降水任务还要面对强度与面积的增消。"
     "② 多尺度、非线性演化。小尺度对流单体生命史只有几十分钟，"
     "大尺度锋面、台风系统则可持续数小时以上，二者同时存在且相互作用，"
     "受动力、热力与微物理过程共同支配，远不是“匀速直线 + 反弹”能刻画的。",
     "③ 真实噪声与观测缺陷。雷达数据含地物杂波、波束遮挡、亮带、标定偏差与非均匀采样，"
     "还存在“回波一边平流一边强度变化”的耦合（advection 与 growth/decay 不可分离），"
     "使单纯的位移估计假设失效；而本实验数据是零噪声、二值化的理想仿真。"
     "④ 评估更严苛。业务上关心的是强降水是否漏报，"
     "常用 POD / FAR / CSI 等分类指标与分强度阈值评估，"
     "而 MSE 会被大范围弱回波主导、对强对流的漏报不敏感，"
     "因此该领域常改用加权损失或“分类 + 回归”混合损失——这也解释了为什么"
     "本实验观察到的“MSE 训练结果偏模糊”在真实业务中必须通过损失设计来纠正。"],
    # (6)
    ["AI 最有价值的环节集中在“可机械翻译”和“形状排错”两类工作上：",
     "① 从公式到代码的落地。我最初的想法是给 4 个门各写一个 Conv2d，"
     "AI 指出 4 个门是同一组输入-状态拼接口，"
     "合并为一次 Conv2d(in+hid, 4*hid) 再 chunk 是等价且更高效的写法，"
     "把 4 次卷积调用压缩成 1 次，代码也更贴近教材公式。"
     "我确认了四个门确实共享同一对输入 X_t 与 H_{t−1}（只是权重不同），"
     "“拼通道后一次卷积再切分”在数学上与原式等价，才采纳了这一改写。"
     "② 张量形状排错。本实验最典型的两类错误——把 (N,T,H,W,1) 直接喂给 conv2d"
     "（报 expected 4D or 5D input）、忘记沿时间维逐帧取 x[:, t]——"
     "都是我按提示词描述后由 AI 在分钟内定位的，省去了大量试错。"
     "③ 解释与交叉验证。用 AI 复核“为什么先算 c_t 再算 h_t、f 门为什么用 sigmoid 而不是 tanh”，"
     "把每一步的数学含义与代码逐行对齐，避免只会跑不会改。",

     "但有三处必须自己读懂公式才可能改对："
     "① 门控顺序。合并卷积把 4*C_h 个通道按 i、f、g、o 切开，"
     "其中 g 用 tanh、i/f/o 用 sigmoid；假如把 g 与 o 的顺序写反，"
     "代码照样能跑、损失照样下降，但语义已经完全错了。"
     "这说明语义正确性无法从“能否跑通”中看出，只能回到公式逐项核对——"
     "这正是 AI 生成的代码最危险的地方。"
     "② 状态传递。ConvLSTM 的 h、c 是二维特征图，初始状态必须 zeros(B, C_h, H, W)；"
     "多层堆叠时上一层输出的 h 要作为下一层该时刻的输入 x，"
     "这一步写错在通道数恰好相同时不会报错，必须靠对结构的理解来检查。"
     "③ 参数量与成本的预判。只有自己按公式手算，"
     "才能在动手前就预判 K=5 会使参数量涨到 2.76 倍、C_h=64 会涨到 3.92 倍、"
     "耗时同步上升，从而判断某个改动是否值得一试。"
     "总体体会：人机协作的理想分工是——AI 负责生成与解释，人负责设计与判断。"
     "AI 把“翻译公式、排查形状、补全绘图”这类体力密集型工作压缩到分钟级，"
     "但它无法替我判断某次改动在语义上是否正确、某个数字是否真实来自本次运行。"
     "AI 生成的代码是起点而不是终点：读懂公式、守住语义、验证每一个数字，仍然是人的责任。"],
]
CONCLUSION_METHOD = (
    "4. 方法论体会：控制变量法 + 固定随机种子是把“MSE 从 0.00xx 降到 0.00yy”这样的数字准确归因到"
    "单一设计点的前提。本实验每次只改一个维度、其余配置与基线完全一致，并且数据合成种子（42）、"
    "参数初始化种子与批内乱序种子全部固定；最优组合再用 42/43/44 三个种子复测取平均，"
    "避免把一次偶然波动当成结论。此外还必须先排除实现层面的隐患——例如指南参考代码中"
    "训练集 data[:2000] 与测试集 data[1800:] 存在 200 条重叠的数据泄漏，"
    "若不先修正，后续所有对比都是在被污染的测试集上进行的，结论毫无意义。")
